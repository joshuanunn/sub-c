(library
 (name subc)
 (modules
  io
  env
  lexer
  lexer_pp
  parser
  type
  ast
  ast_resolution
  ast_flow_label
  ast_type_check
  irgen
  ir
  codegen
  codegen_lower
  codegen_fixup
  asm
  emit)
 (preprocess
  (pps ppx_deriving.show)))

(menhir
 (modules parser))

(ocamllex lexer)
