(library
 (name subc)
 (modules io token lexer parser ast irgen ir codegen asm emit)
 (preprocess
  (pps ppx_deriving.show)))

(menhir
 (modules parser)
 (flags --external-tokens Token))

(ocamllex lexer)
