(Ir.Program
   Ir.Function {name = "main";
     body =
     [Ir.JumpIfNotZero {condition = (Ir.Constant 4); target = "or_true.0"};
       Ir.JumpIfNotZero {condition = (Ir.Constant 0); target = "or_true.0"};
       Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "tmp.0")};
       Ir.Jump {target = "or_end.1"}; (Ir.Label "or_true.0");
       Ir.Copy {src = (Ir.Constant 1); dst = (Ir.Var "tmp.0")};
       (Ir.Label "or_end.1");
       Ir.JumpIfNotZero {condition = (Ir.Constant 0); target = "or_true.2"};
       Ir.JumpIfNotZero {condition = (Ir.Constant 3); target = "or_true.2"};
       Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "tmp.1")};
       Ir.Jump {target = "or_end.3"}; (Ir.Label "or_true.2");
       Ir.Copy {src = (Ir.Constant 1); dst = (Ir.Var "tmp.1")};
       (Ir.Label "or_end.3");
       Ir.Binary {op = Ir.Add; src1 = (Ir.Var "tmp.0");
         src2 = (Ir.Var "tmp.1"); dst = (Ir.Var "tmp.2")};
       Ir.JumpIfNotZero {condition = (Ir.Constant 5); target = "or_true.4"};
       Ir.JumpIfNotZero {condition = (Ir.Constant 5); target = "or_true.4"};
       Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "tmp.3")};
       Ir.Jump {target = "or_end.5"}; (Ir.Label "or_true.4");
       Ir.Copy {src = (Ir.Constant 1); dst = (Ir.Var "tmp.3")};
       (Ir.Label "or_end.5");
       Ir.Binary {op = Ir.Add; src1 = (Ir.Var "tmp.2");
         src2 = (Ir.Var "tmp.3"); dst = (Ir.Var "tmp.4")};
       (Ir.Return (Ir.Var "tmp.4"))]})
