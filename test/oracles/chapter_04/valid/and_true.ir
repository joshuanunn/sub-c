(Ir.Program
   [Ir.Function {name = "main"; global = true; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant -1); dst = (Ir.Var "tmp.0")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.0"); target = "main.and.fl.2"};
        Ir.Copy {src = (Ir.Constant 1); dst = (Ir.Var "tmp.1")};
        Ir.Jump {target = "main.and.en.3"}; (Ir.Label "main.and.fl.2");
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "tmp.1")};
        (Ir.Label "main.and.en.3"); (Ir.Return (Ir.Var "tmp.1"))];
      frame =
      Env.lenv {
        namespace = "main";
        counter = 4;
        offset = -8;
        stack slots = {
          tmp.0 -> -4,
          tmp.1 -> -8,
        }}}
     ])
