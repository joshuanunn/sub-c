(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "add");
         params = [(Ast.Identifier "x.0"); (Ast.Identifier "y.1")];
         body =
         (Some (Ast.Block
                  [(Ast.S
                      (Ast.Return
                         Ast.Binary {op = Ast.Add;
                           left = (Ast.Var (Ast.Identifier "x.0"));
                           right = (Ast.Var (Ast.Identifier "y.1"))}))
                    ]))
         })
     ])
