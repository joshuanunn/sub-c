(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "incr_and_print");
         params = [(Ast.Identifier "c.0")]; body = None });
     (Ast.FunDecl
        { Ast.name = (Ast.Identifier "main"); params = [];
          body =
          (Some (Ast.Block
                   [(Ast.S
                       (Ast.Expression
                          Ast.FunctionCall {
                            name = (Ast.Identifier "incr_and_print");
                            args = [(Ast.LiteralInt 70)]}));
                     (Ast.S (Ast.Return (Ast.LiteralInt 0)))]))
          })
     ])
