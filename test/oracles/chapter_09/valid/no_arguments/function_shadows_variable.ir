(Ir.Program
   [Ir.Function {name = "main"; global = true; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant 3); dst = (Ir.Var "foo.0")};
        Ir.Copy {src = (Ir.Constant 4); dst = (Ir.Var "bar.1")};
        Ir.Binary {op = Ir.Add; src1 = (Ir.Var "foo.0");
          src2 = (Ir.Var "bar.1"); dst = (Ir.Var "tmp.0")};
        Ir.Binary {op = Ir.GreaterThan; src1 = (Ir.Var "tmp.0");
          src2 = (Ir.Constant 0); dst = (Ir.Var "tmp.1")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.1"); target = "main.if.en.2"};
        Ir.FunCall {fun_name = "foo"; args = []; dst = (Ir.Var "tmp.3")};
        Ir.Copy {src = (Ir.Var "tmp.3"); dst = (Ir.Var "bar.1")};
        (Ir.Label "main.if.en.2");
        Ir.Binary {op = Ir.Add; src1 = (Ir.Var "foo.0");
          src2 = (Ir.Var "bar.1"); dst = (Ir.Var "tmp.4")};
        (Ir.Return (Ir.Var "tmp.4")); (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        namespace = "main";
        counter = 5;
        offset = -24;
        stack slots = {
          foo.0 -> -4,
          bar.1 -> -8,
          tmp.0 -> -12,
          tmp.1 -> -16,
          tmp.3 -> -20,
          tmp.4 -> -24,
        }}};
     Ir.Function {name = "foo"; global = true; params = [];
       body = [(Ir.Return (Ir.Constant 8)); (Ir.Return (Ir.Constant 0))];
       frame =
       Env.lenv {
         namespace = "foo";
         counter = 0;
         offset = 0;
         stack slots = {
         }}}
     ])
