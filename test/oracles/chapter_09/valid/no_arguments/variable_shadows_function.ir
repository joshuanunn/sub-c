(Ir.Program
   [Ir.Function {name = "main"; params = [];
      body =
      [Ir.FunCall {fun_name = "foo"; args = []; dst = (Ir.Var "tmp.0")};
        Ir.Copy {src = (Ir.Var "tmp.0"); dst = (Ir.Var "x.0")};
        Ir.Binary {op = Ir.GreaterThan; src1 = (Ir.Var "x.0");
          src2 = (Ir.Constant 0); dst = (Ir.Var "tmp.1")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.1"); target = "if.en.2"};
        Ir.Copy {src = (Ir.Constant 3); dst = (Ir.Var "foo.1")};
        Ir.Binary {op = Ir.Add; src1 = (Ir.Var "x.0");
          src2 = (Ir.Var "foo.1"); dst = (Ir.Var "tmp.3")};
        Ir.Copy {src = (Ir.Var "tmp.3"); dst = (Ir.Var "x.0")};
        (Ir.Label "if.en.2"); (Ir.Return (Ir.Var "x.0"));
        (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        counter = 4;
        offset = -20;
        stack slots = {
          x.0   -> -4,
          tmp.0 -> -8,
          tmp.1 -> -12,
          foo.1 -> -16,
          tmp.3 -> -20,
        }}};
     Ir.Function {name = "foo"; params = [];
       body = [(Ir.Return (Ir.Constant 4)); (Ir.Return (Ir.Constant 0))];
       frame = Env.lenv {
                 counter = 0;
                 offset = 0;
                 stack slots = {
                 }}}
     ])
