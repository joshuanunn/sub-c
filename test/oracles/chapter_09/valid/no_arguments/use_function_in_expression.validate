(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "bar"); params = [];
         body = (Some (Ast.Block [(Ast.S (Ast.Return (Ast.LiteralInt 9)))]))
         });
     (Ast.FunDecl
        { Ast.name = (Ast.Identifier "foo"); params = [];
          body =
          (Some (Ast.Block
                   [(Ast.S
                       (Ast.Return
                          Ast.Binary {op = Ast.Multiply;
                            left = (Ast.LiteralInt 2);
                            right =
                            Ast.FunctionCall {name = (Ast.Identifier "bar");
                              args = []}}))
                     ]))
          });
     (Ast.FunDecl
        { Ast.name = (Ast.Identifier "main"); params = [];
          body =
          (Some (Ast.Block
                   [(Ast.S
                       (Ast.Return
                          Ast.Binary {op = Ast.Add;
                            left =
                            Ast.FunctionCall {name = (Ast.Identifier "foo");
                              args = []};
                            right =
                            Ast.Binary {op = Ast.Divide;
                              left =
                              Ast.FunctionCall {
                                name = (Ast.Identifier "bar"); args = []};
                              right = (Ast.LiteralInt 3)}}))
                     ]))
          })
     ])
