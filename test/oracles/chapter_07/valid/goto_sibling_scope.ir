(Ir.Program
   [Ir.Function {name = "main"; global = true; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "sum.2")};
        Ir.Copy {src = (Ir.Constant 5); dst = (Ir.Var "a.3")};
        Ir.Jump {target = "other_if.1"}; (Ir.Label "first_if.0");
        Ir.Binary {op = Ir.Add; src1 = (Ir.Constant 6);
          src2 = (Ir.Var "a.3"); dst = (Ir.Var "tmp.1")};
        Ir.Copy {src = (Ir.Var "tmp.1"); dst = (Ir.Var "sum.2")};
        Ir.Jump {target = "main.if.en.2"}; (Ir.Label "other_if.1");
        Ir.Copy {src = (Ir.Constant 6); dst = (Ir.Var "a.4")};
        Ir.Copy {src = (Ir.Constant 6); dst = (Ir.Var "tmp.3")};
        Ir.Copy {src = (Ir.Constant 6); dst = (Ir.Var "sum.2")};
        Ir.Jump {target = "first_if.0"}; (Ir.Label "main.if.en.2");
        (Ir.Return (Ir.Var "tmp.1"))];
      frame =
      Env.lenv {
        namespace = "main";
        counter = 4;
        offset = -20;
        stack slots = {
          sum.2 -> -4,
          a.3   -> -8,
          tmp.1 -> -12,
          a.4   -> -16,
          tmp.3 -> -20,
        }}}
     ])
