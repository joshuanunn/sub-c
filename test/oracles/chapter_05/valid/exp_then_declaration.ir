(Ir.Program
   [Ir.Function {name = "main"; global = true; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant -2593); dst = (Ir.Var "tmp.0")};
        Ir.Copy {src = (Ir.Var "tmp.0"); dst = (Ir.Var "a.0")};
        Ir.Binary {op = Ir.Remainder; src1 = (Ir.Var "a.0");
          src2 = (Ir.Constant 3); dst = (Ir.Var "tmp.1")};
        Ir.Copy {src = (Ir.Var "tmp.1"); dst = (Ir.Var "a.0")};
        Ir.Unary {op = Ir.Negate; src = (Ir.Var "a.0");
          dst = (Ir.Var "tmp.2")};
        Ir.Copy {src = (Ir.Var "tmp.2"); dst = (Ir.Var "b.1")};
        (Ir.Return (Ir.Var "b.1")); (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        counter = 3;
        offset = -20;
        stack slots = {
          a.0   -> -4,
          tmp.0 -> -8,
          tmp.1 -> -12,
          b.1   -> -16,
          tmp.2 -> -20,
        }}}
     ])
