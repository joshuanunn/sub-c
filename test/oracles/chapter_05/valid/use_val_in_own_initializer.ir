(Ir.Program
   [Ir.Function {name = "main"; params = [];
      body =
      [Ir.JumpIfZero {condition = (Ir.Constant 0); target = "and.fl.1"};
        Ir.JumpIfZero {condition = (Ir.Var "a.0"); target = "and.fl.1"};
        Ir.Copy {src = (Ir.Constant 1); dst = (Ir.Var "tmp.0")};
        Ir.Jump {target = "and.en.2"}; (Ir.Label "and.fl.1");
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "tmp.0")};
        (Ir.Label "and.en.2");
        Ir.Copy {src = (Ir.Var "tmp.0"); dst = (Ir.Var "a.0")};
        (Ir.Return (Ir.Var "a.0")); (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        counter = 3;
        offset = -8;
        stack slots = {
          a.0   -> -4,
          tmp.0 -> -8,
        }}}
     ])
