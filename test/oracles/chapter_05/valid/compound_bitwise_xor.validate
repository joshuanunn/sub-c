(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "main"); params = [];
         body =
         (Some (Ast.Block
                  [(Ast.D
                      (Ast.VarDecl
                         { Ast.name = (Ast.Identifier "to_xor.0");
                           init = (Some (Ast.LiteralInt 7)); storage = None }));
                    (Ast.S
                       (Ast.Expression
                          (Ast.Assignment (
                             (Ast.Var (Ast.Identifier "to_xor.0")),
                             Ast.Binary {op = Ast.BwXor;
                               left = (Ast.Var (Ast.Identifier "to_xor.0"));
                               right = (Ast.LiteralInt 5)}
                             ))));
                    (Ast.S (Ast.Return (Ast.Var (Ast.Identifier "to_xor.0"))))
                    ]));
         storage = None })
     ])
