(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "main"); params = [];
         body =
         (Some (Ast.Block
                  [(Ast.D
                      (Ast.VarDecl
                         { Ast.name = (Ast.Identifier "to_subtract.0");
                           init = (Some (Ast.LiteralInt 10)) }));
                    (Ast.S
                       (Ast.Expression
                          (Ast.Assignment (
                             (Ast.Var (Ast.Identifier "to_subtract.0")),
                             Ast.Binary {op = Ast.Subtract;
                               left =
                               (Ast.Var (Ast.Identifier "to_subtract.0"));
                               right = (Ast.LiteralInt 8)}
                             ))));
                    (Ast.S
                       (Ast.Return (Ast.Var (Ast.Identifier "to_subtract.0"))))
                    ]))
         })
     ])
