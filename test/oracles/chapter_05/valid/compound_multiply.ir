(Ir.Program
   [Ir.Function {name = "main"; global = true; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant 4); dst = (Ir.Var "to_multiply.0")};
        Ir.Binary {op = Ir.Multiply; src1 = (Ir.Var "to_multiply.0");
          src2 = (Ir.Constant 3); dst = (Ir.Var "tmp.0")};
        Ir.Copy {src = (Ir.Var "tmp.0"); dst = (Ir.Var "to_multiply.0")};
        (Ir.Return (Ir.Var "to_multiply.0"))];
      frame =
      Env.lenv {
        namespace = "main";
        counter = 1;
        offset = -8;
        stack slots = {
          to_multiply.0 -> -4,
          tmp.0         -> -8,
        }}}
     ])
