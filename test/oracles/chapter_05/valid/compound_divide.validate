(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "main"); params = [];
         body =
         (Some (Ast.Block
                  [(Ast.D
                      (Ast.VarDecl
                         { Ast.name = (Ast.Identifier "to_divide.0");
                           init = (Some (Ast.LiteralInt 8)); storage = None }));
                    (Ast.S
                       (Ast.Expression
                          (Ast.Assignment (
                             (Ast.Var (Ast.Identifier "to_divide.0")),
                             Ast.Binary {op = Ast.Divide;
                               left =
                               (Ast.Var (Ast.Identifier "to_divide.0"));
                               right = (Ast.LiteralInt 4)}
                             ))));
                    (Ast.S
                       (Ast.Return (Ast.Var (Ast.Identifier "to_divide.0"))))
                    ]));
         storage = None })
     ])
