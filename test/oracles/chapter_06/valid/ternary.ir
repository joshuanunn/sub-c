(Ir.Program
   [Ir.Function {name = "main"; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "a.0")};
        Ir.Unary {op = Ir.Negate; src = (Ir.Constant 1);
          dst = (Ir.Var "tmp.1")};
        Ir.Binary {op = Ir.GreaterThan; src1 = (Ir.Var "a.0");
          src2 = (Ir.Var "tmp.1"); dst = (Ir.Var "tmp.2")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.2"); target = "cond.el.4"};
        Ir.Copy {src = (Ir.Constant 4); dst = (Ir.Var "tmp.0")};
        Ir.Jump {target = "cond.en.3"}; (Ir.Label "cond.el.4");
        Ir.Copy {src = (Ir.Constant 5); dst = (Ir.Var "tmp.0")};
        (Ir.Label "cond.en.3"); (Ir.Return (Ir.Var "tmp.0"));
        (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        counter = 5;
        offset = -16;
        stack slots = {
          a.0   -> -4,
          tmp.0 -> -8,
          tmp.1 -> -12,
          tmp.2 -> -16,
        }}}
     ])
