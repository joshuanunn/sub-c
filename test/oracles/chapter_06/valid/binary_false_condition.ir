(Ir.Program
   [Ir.Function {name = "main"; params = [];
      body =
      [Ir.Binary {op = Ir.Add; src1 = (Ir.Constant 1);
         src2 = (Ir.Constant 2); dst = (Ir.Var "tmp.0")};
        Ir.Binary {op = Ir.Equal; src1 = (Ir.Var "tmp.0");
          src2 = (Ir.Constant 4); dst = (Ir.Var "tmp.1")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.1"); target = "if.en.2"};
        (Ir.Return (Ir.Constant 5)); (Ir.Label "if.en.2");
        (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        counter = 3;
        offset = -8;
        stack slots = {
          tmp.0 -> -4,
          tmp.1 -> -8,
        }}}
     ])
