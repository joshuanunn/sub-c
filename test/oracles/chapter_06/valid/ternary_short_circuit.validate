(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "main"); params = [];
         body =
         (Some (Ast.Block
                  [(Ast.D
                      (Ast.VarDecl
                         { Ast.name = (Ast.Identifier "a.0");
                           init = (Some (Ast.LiteralInt 1)); storage = None }));
                    (Ast.D
                       (Ast.VarDecl
                          { Ast.name = (Ast.Identifier "b.1");
                            init = (Some (Ast.LiteralInt 0)); storage = None
                            }));
                    (Ast.S
                       (Ast.Expression
                          Ast.Conditional {
                            cond_exp = (Ast.Var (Ast.Identifier "a.0"));
                            then_exp =
                            (Ast.Assignment (
                               (Ast.Var (Ast.Identifier "b.1")),
                               (Ast.LiteralInt 1)));
                            else_exp =
                            (Ast.Assignment (
                               (Ast.Var (Ast.Identifier "b.1")),
                               (Ast.LiteralInt 2)))}));
                    (Ast.S (Ast.Return (Ast.Var (Ast.Identifier "b.1"))))]));
         storage = None })
     ])
