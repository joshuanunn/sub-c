(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "main"); params = [];
         body =
         (Some (Ast.Block
                  [(Ast.D
                      (Ast.VarDecl
                         { Ast.name = (Ast.Identifier "a.0");
                           init = (Some (Ast.LiteralInt 0)) }));
                    (Ast.S
                       Ast.If {
                         cond_exp =
                         (Ast.Assignment ((Ast.Var (Ast.Identifier "a.0")),
                            Ast.Binary {op = Ast.Add;
                              left = (Ast.Var (Ast.Identifier "a.0"));
                              right = (Ast.LiteralInt 1)}
                            ));
                         then_smt =
                         (Ast.Return (Ast.Var (Ast.Identifier "a.0")));
                         else_smt = None});
                    (Ast.S (Ast.Return (Ast.LiteralInt 10)))]))
         })
     ])
