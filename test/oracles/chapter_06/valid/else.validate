(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "main"); params = [];
         body =
         (Some (Ast.Block
                  [(Ast.D
                      (Ast.VarDecl
                         { Ast.name = (Ast.Identifier "a.0");
                           init = (Some (Ast.LiteralInt 0)) }));
                    (Ast.S
                       Ast.If {cond_exp = (Ast.Var (Ast.Identifier "a.0"));
                         then_smt = (Ast.Return (Ast.LiteralInt 1));
                         else_smt = (Some (Ast.Return (Ast.LiteralInt 2)))})
                    ]))
         })
     ])
