(Ir.Program
   [Ir.Function {name = "main"; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant 1); dst = (Ir.Var "a.0")};
        Ir.Binary {op = Ir.NotEqual; src1 = (Ir.Var "a.0");
          src2 = (Ir.Constant 2); dst = (Ir.Var "tmp.1")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.1"); target = "cond.el.3"};
        Ir.Copy {src = (Ir.Constant 2); dst = (Ir.Var "a.0")};
        Ir.Copy {src = (Ir.Var "a.0"); dst = (Ir.Var "tmp.0")};
        Ir.Jump {target = "cond.en.2"}; (Ir.Label "cond.el.3");
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "tmp.0")};
        (Ir.Label "cond.en.2"); (Ir.Return (Ir.Var "a.0"));
        (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        counter = 4;
        offset = -12;
        stack slots = {
          a.0   -> -4,
          tmp.0 -> -8,
          tmp.1 -> -12,
        }}}
     ])
