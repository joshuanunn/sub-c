(Ir.Program
   Ir.Function {name = "main";
     body =
     [Ir.Copy {src = (Ir.Constant 1); dst = (Ir.Var "a.5")};
       (Ir.Label "label_if");
       Ir.JumpIfZero {condition = (Ir.Var "a.5"); target = "if.el.1"};
       Ir.Jump {target = "label_expression"}; Ir.Jump {target = "if.en.0"};
       (Ir.Label "if.el.1"); Ir.Jump {target = "label_empty"};
       (Ir.Label "if.en.0"); (Ir.Label "label_goto");
       Ir.Jump {target = "label_return"};
       Ir.JumpIfZero {condition = (Ir.Constant 0); target = "if.en.2"};
       (Ir.Label "label_expression");
       Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "a.5")};
       (Ir.Label "if.en.2"); Ir.Jump {target = "label_if"};
       (Ir.Label "label_return"); (Ir.Return (Ir.Var "a.5"));
       (Ir.Label "label_empty");
       Ir.Copy {src = (Ir.Constant 100); dst = (Ir.Var "a.5")};
       Ir.Jump {target = "label_goto"}; (Ir.Return (Ir.Constant 0))]})
