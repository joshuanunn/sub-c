(Ir.Program
   [Ir.Function {name = "main"; global = true; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant 10); dst = (Ir.Var "a.0")};
        Ir.JumpIfNotZero {condition = (Ir.Var "a.0"); target = "or.tr.2"};
        Ir.JumpIfNotZero {condition = (Ir.Constant 0); target = "or.tr.2"};
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "tmp.1")};
        Ir.Jump {target = "or.en.3"}; (Ir.Label "or.tr.2");
        Ir.Copy {src = (Ir.Constant 1); dst = (Ir.Var "tmp.1")};
        (Ir.Label "or.en.3");
        Ir.JumpIfZero {condition = (Ir.Var "tmp.1"); target = "cond.el.5"};
        Ir.Copy {src = (Ir.Constant 20); dst = (Ir.Var "tmp.0")};
        Ir.Jump {target = "cond.en.4"}; (Ir.Label "cond.el.5");
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "tmp.0")};
        (Ir.Label "cond.en.4"); (Ir.Return (Ir.Var "tmp.0"));
        (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        counter = 6;
        offset = -12;
        stack slots = {
          a.0   -> -4,
          tmp.0 -> -8,
          tmp.1 -> -12,
        }}}
     ])
