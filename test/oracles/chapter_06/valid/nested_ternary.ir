(Ir.Program
   [Ir.Function {name = "main"; global = true; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant 1); dst = (Ir.Var "a.0")};
        Ir.Copy {src = (Ir.Constant 2); dst = (Ir.Var "b.1")};
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "flag.2")};
        Ir.Binary {op = Ir.GreaterThan; src1 = (Ir.Var "a.0");
          src2 = (Ir.Var "b.1"); dst = (Ir.Var "tmp.1")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.1"); target = "cond.el.3"};
        Ir.Copy {src = (Ir.Constant 5); dst = (Ir.Var "tmp.0")};
        Ir.Jump {target = "cond.en.2"}; (Ir.Label "cond.el.3");
        Ir.JumpIfZero {condition = (Ir.Var "flag.2"); target = "cond.el.6"};
        Ir.Copy {src = (Ir.Constant 6); dst = (Ir.Var "tmp.4")};
        Ir.Jump {target = "cond.en.5"}; (Ir.Label "cond.el.6");
        Ir.Copy {src = (Ir.Constant 7); dst = (Ir.Var "tmp.4")};
        (Ir.Label "cond.en.5");
        Ir.Copy {src = (Ir.Var "tmp.4"); dst = (Ir.Var "tmp.0")};
        (Ir.Label "cond.en.2"); (Ir.Return (Ir.Var "tmp.0"));
        (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        counter = 7;
        offset = -24;
        stack slots = {
          a.0    -> -4,
          b.1    -> -8,
          flag.2 -> -12,
          tmp.0  -> -16,
          tmp.1  -> -20,
          tmp.4  -> -24,
        }}}
     ])
