(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "main"); params = [];
         body =
         (Some (Ast.Block
                  [(Ast.S
                      Ast.If {
                        cond_exp =
                        Ast.Binary {op = Ast.Equal;
                          left =
                          Ast.Binary {op = Ast.Add;
                            left = (Ast.LiteralInt 1);
                            right = (Ast.LiteralInt 2)};
                          right = (Ast.LiteralInt 3)};
                        then_smt = (Ast.Return (Ast.LiteralInt 5));
                        else_smt = None})
                    ]));
         storage = None })
     ])
