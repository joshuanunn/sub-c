(Ir.Program
   [Ir.Function {name = "main"; global = true; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "sum.1")};
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "i.2")};
        (Ir.Label "loop.st.1"); (Ir.Label "lbl.0");
        Ir.Binary {op = Ir.Add; src1 = (Ir.Var "sum.1");
          src2 = (Ir.Constant 1); dst = (Ir.Var "tmp.0")};
        Ir.Copy {src = (Ir.Var "tmp.0"); dst = (Ir.Var "sum.1")};
        Ir.Binary {op = Ir.Add; src1 = (Ir.Var "i.2");
          src2 = (Ir.Constant 1); dst = (Ir.Var "tmp.1")};
        Ir.Copy {src = (Ir.Var "tmp.1"); dst = (Ir.Var "i.2")};
        Ir.Binary {op = Ir.GreaterThan; src1 = (Ir.Var "i.2");
          src2 = (Ir.Constant 10); dst = (Ir.Var "tmp.2")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.2"); target = "if.en.3"};
        Ir.Jump {target = "loop.br.1"}; (Ir.Label "if.en.3");
        Ir.Jump {target = "lbl.0"}; (Ir.Label "loop.ct.1");
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "i.2")};
        Ir.Jump {target = "loop.st.1"}; (Ir.Label "loop.br.1");
        (Ir.Return (Ir.Var "sum.1")); (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        counter = 4;
        offset = -20;
        stack slots = {
          sum.1 -> -4,
          i.2   -> -8,
          tmp.0 -> -12,
          tmp.1 -> -16,
          tmp.2 -> -20,
        }}}
     ])
