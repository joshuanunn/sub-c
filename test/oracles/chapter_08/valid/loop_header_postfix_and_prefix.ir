(Ir.Program
   [Ir.Function {name = "main"; global = true; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant 100); dst = (Ir.Var "i.0")};
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "count.1")};
        (Ir.Label "loop.ct.1");
        Ir.Copy {src = (Ir.Var "i.0"); dst = (Ir.Var "tmp.0")};
        Ir.Binary {op = Ir.Subtract; src1 = (Ir.Var "i.0");
          src2 = (Ir.Constant 1); dst = (Ir.Var "i.0")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.0"); target = "loop.br.1"};
        Ir.Copy {src = (Ir.Var "count.1"); dst = (Ir.Var "tmp.1")};
        Ir.Binary {op = Ir.Add; src1 = (Ir.Var "count.1");
          src2 = (Ir.Constant 1); dst = (Ir.Var "count.1")};
        Ir.Jump {target = "loop.ct.1"}; (Ir.Label "loop.br.1");
        Ir.Binary {op = Ir.NotEqual; src1 = (Ir.Var "count.1");
          src2 = (Ir.Constant 100); dst = (Ir.Var "tmp.2")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.2"); target = "main.if.en.3"};
        (Ir.Return (Ir.Constant 0)); (Ir.Label "main.if.en.3");
        Ir.Copy {src = (Ir.Constant 100); dst = (Ir.Var "i.0")};
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "count.1")};
        (Ir.Label "loop.ct.2");
        Ir.Binary {op = Ir.Subtract; src1 = (Ir.Var "i.0");
          src2 = (Ir.Constant 1); dst = (Ir.Var "i.0")};
        Ir.JumpIfZero {condition = (Ir.Var "i.0"); target = "loop.br.2"};
        Ir.Copy {src = (Ir.Var "count.1"); dst = (Ir.Var "tmp.4")};
        Ir.Binary {op = Ir.Add; src1 = (Ir.Var "count.1");
          src2 = (Ir.Constant 1); dst = (Ir.Var "count.1")};
        Ir.Jump {target = "loop.ct.2"}; (Ir.Label "loop.br.2");
        Ir.Binary {op = Ir.NotEqual; src1 = (Ir.Var "count.1");
          src2 = (Ir.Constant 99); dst = (Ir.Var "tmp.5")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.5"); target = "main.if.en.6"};
        (Ir.Return (Ir.Constant 0)); (Ir.Label "main.if.en.6");
        (Ir.Return (Ir.Constant 1)); (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        namespace = "main";
        counter = 7;
        offset = -28;
        stack slots = {
          i.0     -> -4,
          count.1 -> -8,
          tmp.0   -> -12,
          tmp.1   -> -16,
          tmp.2   -> -20,
          tmp.4   -> -24,
          tmp.5   -> -28,
        }}}
     ])
