(Ir.Program
   [Ir.Function {name = "main"; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant 10); dst = (Ir.Var "cond.0")};
        Ir.Binary {op = Ir.Equal; src1 = (Ir.Var "cond.0");
          src2 = (Ir.Constant 1); dst = (Ir.Var "tmp.0")};
        Ir.JumpIfNotZero {condition = (Ir.Var "tmp.0");
          target = "swit.cs.1.1"};
        Ir.Binary {op = Ir.Equal; src1 = (Ir.Var "cond.0");
          src2 = (Ir.Constant 10); dst = (Ir.Var "tmp.1")};
        Ir.JumpIfNotZero {condition = (Ir.Var "tmp.1");
          target = "swit.cs.1.10"};
        Ir.Jump {target = "swit.df.1"}; (Ir.Label "swit.cs.1.1");
        (Ir.Return (Ir.Constant 0)); (Ir.Label "swit.cs.1.10");
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "i.1")};
        (Ir.Label "loop.st.2");
        Ir.Binary {op = Ir.LessThan; src1 = (Ir.Var "i.1");
          src2 = (Ir.Constant 5); dst = (Ir.Var "tmp.2")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.2"); target = "loop.br.2"};
        Ir.Binary {op = Ir.Subtract; src1 = (Ir.Var "cond.0");
          src2 = (Ir.Constant 1); dst = (Ir.Var "tmp.3")};
        Ir.Copy {src = (Ir.Var "tmp.3"); dst = (Ir.Var "cond.0")};
        Ir.Binary {op = Ir.Equal; src1 = (Ir.Var "cond.0");
          src2 = (Ir.Constant 8); dst = (Ir.Var "tmp.4")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.4"); target = "if.en.5"};
        Ir.Jump {target = "loop.br.2"}; (Ir.Label "if.en.5");
        (Ir.Label "loop.ct.2");
        Ir.Binary {op = Ir.Add; src1 = (Ir.Var "i.1");
          src2 = (Ir.Constant 1); dst = (Ir.Var "tmp.6")};
        Ir.Copy {src = (Ir.Var "tmp.6"); dst = (Ir.Var "i.1")};
        Ir.Jump {target = "loop.st.2"}; (Ir.Label "loop.br.2");
        (Ir.Return (Ir.Constant 123)); (Ir.Label "swit.df.1");
        (Ir.Return (Ir.Constant 2)); (Ir.Label "swit.br.1");
        (Ir.Return (Ir.Constant 3)); (Ir.Return (Ir.Constant 0))]}
     ])
