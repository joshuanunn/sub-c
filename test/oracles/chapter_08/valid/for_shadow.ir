(Ir.Program
   [Ir.Function {name = "main"; global = true; params = [];
      body =
      [Ir.Copy {src = (Ir.Constant 1); dst = (Ir.Var "shadow.0")};
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "acc.1")};
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "shadow.2")};
        (Ir.Label "loop.st.1");
        Ir.Binary {op = Ir.LessThan; src1 = (Ir.Var "shadow.2");
          src2 = (Ir.Constant 10); dst = (Ir.Var "tmp.0")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.0"); target = "loop.br.1"};
        Ir.Binary {op = Ir.Add; src1 = (Ir.Var "acc.1");
          src2 = (Ir.Var "shadow.2"); dst = (Ir.Var "tmp.1")};
        Ir.Copy {src = (Ir.Var "tmp.1"); dst = (Ir.Var "acc.1")};
        (Ir.Label "loop.ct.1");
        Ir.Binary {op = Ir.Add; src1 = (Ir.Var "shadow.2");
          src2 = (Ir.Constant 1); dst = (Ir.Var "tmp.2")};
        Ir.Copy {src = (Ir.Var "tmp.2"); dst = (Ir.Var "shadow.2")};
        Ir.Jump {target = "loop.st.1"}; (Ir.Label "loop.br.1");
        Ir.Binary {op = Ir.Equal; src1 = (Ir.Var "acc.1");
          src2 = (Ir.Constant 45); dst = (Ir.Var "tmp.3")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.3"); target = "and.fl.6"};
        Ir.Binary {op = Ir.Equal; src1 = (Ir.Var "shadow.0");
          src2 = (Ir.Constant 1); dst = (Ir.Var "tmp.4")};
        Ir.JumpIfZero {condition = (Ir.Var "tmp.4"); target = "and.fl.6"};
        Ir.Copy {src = (Ir.Constant 1); dst = (Ir.Var "tmp.5")};
        Ir.Jump {target = "and.en.7"}; (Ir.Label "and.fl.6");
        Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "tmp.5")};
        (Ir.Label "and.en.7"); (Ir.Return (Ir.Var "tmp.5"));
        (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        counter = 8;
        offset = -36;
        stack slots = {
          shadow.0 -> -4,
          acc.1    -> -8,
          shadow.2 -> -12,
          tmp.0    -> -16,
          tmp.1    -> -20,
          tmp.2    -> -24,
          tmp.3    -> -28,
          tmp.4    -> -32,
          tmp.5    -> -36,
        }}}
     ])
