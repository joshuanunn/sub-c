(Ir.Program
   Ir.Function {name = "main";
     body =
     [Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "result.1")};
       Ir.Jump {target = "label"}; (Ir.Label "continue.while.1");
       Ir.JumpIfZero {condition = (Ir.Constant 0); target = "break.while.1"};
       (Ir.Label "label");
       Ir.Copy {src = (Ir.Constant 1); dst = (Ir.Var "result.1")};
       Ir.Jump {target = "continue.while.1"}; (Ir.Label "break.while.1");
       (Ir.Return (Ir.Var "result.1")); (Ir.Return (Ir.Constant 0))]})
