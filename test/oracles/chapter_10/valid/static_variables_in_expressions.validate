(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "main"); params = [];
         body =
         (Some (Ast.Block
                  [(Ast.D
                      (Ast.VarDecl
                         { Ast.name = (Ast.Identifier "i.0");
                           init = (Some (Ast.LiteralInt 2));
                           storage = (Some Ast.Static) }));
                    (Ast.D
                       (Ast.VarDecl
                          { Ast.name = (Ast.Identifier "j.1");
                            init = (Some (Ast.LiteralInt 3));
                            storage = (Some Ast.Static) }));
                    (Ast.D
                       (Ast.VarDecl
                          { Ast.name = (Ast.Identifier "cmp.2");
                            init =
                            (Some Ast.Binary {op = Ast.LessThan;
                                    left = (Ast.Var (Ast.Identifier "i.0"));
                                    right = (Ast.Var (Ast.Identifier "j.1"))});
                            storage = None }));
                    (Ast.S
                       Ast.If {
                         cond_exp =
                         Ast.Unary {op = Ast.Not;
                           exp = (Ast.Var (Ast.Identifier "cmp.2"))};
                         then_smt = (Ast.Return (Ast.LiteralInt 1));
                         else_smt = None});
                    (Ast.S (Ast.Return (Ast.LiteralInt 0)))]));
         storage = None })
     ])
