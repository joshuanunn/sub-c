(Ast.Program
   [(Ast.VarDecl
       { Ast.name = (Ast.Identifier "foo"); init = None;
         storage = (Some Ast.Extern) });
     (Ast.VarDecl
        { Ast.name = (Ast.Identifier "foo"); init = None; storage = None });
     (Ast.VarDecl
        { Ast.name = (Ast.Identifier "foo"); init = None; storage = None });
     (Ast.FunDecl
        { Ast.name = (Ast.Identifier "main"); params = [];
          body =
          (Some (Ast.Block
                   [(Ast.S
                       Ast.For {
                         init =
                         (Ast.InclDecl
                            { Ast.name = (Ast.Identifier "i.0");
                              init = (Some (Ast.LiteralInt 0));
                              storage = None });
                         cond =
                         (Some Ast.Binary {op = Ast.LessThan;
                                 left = (Ast.Var (Ast.Identifier "i.0"));
                                 right = (Ast.LiteralInt 5)});
                         post =
                         (Some (Ast.Assignment (
                                  (Ast.Var (Ast.Identifier "i.0")),
                                  Ast.Binary {op = Ast.Add;
                                    left = (Ast.Var (Ast.Identifier "i.0"));
                                    right = (Ast.LiteralInt 1)}
                                  )));
                         body =
                         (Ast.Expression
                            (Ast.Assignment (
                               (Ast.Var (Ast.Identifier "foo")),
                               Ast.Binary {op = Ast.Add;
                                 left = (Ast.Var (Ast.Identifier "foo"));
                                 right = (Ast.LiteralInt 1)}
                               )));
                         id = (Some (Ast.LoopLabel "1"))});
                     (Ast.S (Ast.Return (Ast.Var (Ast.Identifier "foo"))))]));
          storage = None });
     (Ast.VarDecl
        { Ast.name = (Ast.Identifier "foo"); init = None; storage = None })
     ])
