(Ir.Program
   [Ir.Function {name = "callee"; global = true; params = [];
      body =
      [Ir.Binary {op = Ir.Divide; src1 = (Ir.Constant 1);
         src2 = (Ir.Constant 0); dst = (Ir.Var "tmp.0")};
        (Ir.Return (Ir.Var "tmp.0"))];
      frame =
      Env.lenv {
        namespace = "callee";
        counter = 1;
        offset = -4;
        stack slots = {
          tmp.0 -> -4,
        }}};
     Ir.Function {name = "target"; global = true; params = [];
       body =
       [Ir.Copy {src = (Ir.Constant 105); dst = (Ir.Var "result.0")};
         Ir.Copy {src = (Ir.Constant 0); dst = (Ir.Var "i.1")};
         (Ir.Label "loop.st.1");
         Ir.Binary {op = Ir.LessThan; src1 = (Ir.Var "i.1");
           src2 = (Ir.Constant 100); dst = (Ir.Var "tmp.0")};
         Ir.JumpIfZero {condition = (Ir.Var "tmp.0"); target = "loop.br.1"};
         Ir.Binary {op = Ir.Subtract; src1 = (Ir.Var "result.0");
           src2 = (Ir.Var "i.1"); dst = (Ir.Var "tmp.3")};
         Ir.Copy {src = (Ir.Var "tmp.3"); dst = (Ir.Var "result.0")};
         Ir.Binary {op = Ir.Add; src1 = (Ir.Var "i.1");
           src2 = (Ir.Constant 1); dst = (Ir.Var "tmp.4")};
         Ir.Copy {src = (Ir.Var "tmp.4"); dst = (Ir.Var "i.1")};
         Ir.Jump {target = "loop.st.1"}; (Ir.Label "loop.br.1");
         (Ir.Return (Ir.Var "result.0"))];
       frame =
       Env.lenv {
         namespace = "target";
         counter = 5;
         offset = -24;
         stack slots = {
           result.0 -> -4,
           i.1      -> -8,
           tmp.0    -> -12,
           tmp.2    -> -16,
           tmp.3    -> -20,
           tmp.4    -> -24,
         }}};
     Ir.Function {name = "main"; global = true; params = [];
       body =
       [Ir.FunCall {fun_name = "target"; args = []; dst = (Ir.Var "tmp.0")};
         Ir.Copy {src = (Ir.Constant -4845); dst = (Ir.Var "tmp.1")};
         Ir.Binary {op = Ir.NotEqual; src1 = (Ir.Var "tmp.0");
           src2 = (Ir.Var "tmp.1"); dst = (Ir.Var "tmp.2")};
         Ir.JumpIfZero {condition = (Ir.Var "tmp.2"); target = "main.if.en.3"};
         (Ir.Return (Ir.Constant 1)); (Ir.Label "main.if.en.3");
         (Ir.Return (Ir.Constant 0))];
       frame =
       Env.lenv {
         namespace = "main";
         counter = 4;
         offset = -12;
         stack slots = {
           tmp.0 -> -4,
           tmp.1 -> -8,
           tmp.2 -> -12,
         }}}
     ])
