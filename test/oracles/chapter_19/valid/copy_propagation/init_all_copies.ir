(Ir.Program
   [Ir.Function {name = "increment_counter"; global = true; params = [];
      body =
      [Ir.Binary {op = Ir.Add; src1 = (Ir.Var "counter");
         src2 = (Ir.Constant 1); dst = (Ir.Var "tmp.0")};
        Ir.Copy {src = (Ir.Var "tmp.0"); dst = (Ir.Var "counter")};
        (Ir.Return (Ir.Constant 0))];
      frame =
      Env.lenv {
        namespace = "increment_counter";
        counter = 1;
        offset = -4;
        stack slots = {
          tmp.0 -> -4,
        }}};
     Ir.Function {name = "target"; global = true; params = [];
       body =
       [Ir.Copy {src = (Ir.Constant 3); dst = (Ir.Var "y.0")};
         (Ir.Label "loop.st.1");
         Ir.FunCall {fun_name = "increment_counter"; args = [];
           dst = (Ir.Var "tmp.0")};
         Ir.Binary {op = Ir.LessThan; src1 = (Ir.Var "counter");
           src2 = (Ir.Constant 5); dst = (Ir.Var "tmp.1")};
         Ir.JumpIfNotZero {condition = (Ir.Var "tmp.1"); target = "loop.st.1"};
         (Ir.Return (Ir.Constant 3))];
       frame =
       Env.lenv {
         namespace = "target";
         counter = 2;
         offset = -12;
         stack slots = {
           y.0   -> -4,
           tmp.0 -> -8,
           tmp.1 -> -12,
         }}};
     Ir.Function {name = "main"; global = true; params = [];
       body =
       [Ir.FunCall {fun_name = "target"; args = []; dst = (Ir.Var "tmp.0")};
         Ir.Copy {src = (Ir.Var "tmp.0"); dst = (Ir.Var "result.1")};
         Ir.Binary {op = Ir.NotEqual; src1 = (Ir.Var "tmp.0");
           src2 = (Ir.Constant 3); dst = (Ir.Var "tmp.1")};
         Ir.JumpIfZero {condition = (Ir.Var "tmp.1"); target = "main.if.en.2"};
         (Ir.Return (Ir.Constant 1)); (Ir.Label "main.if.en.2");
         Ir.Binary {op = Ir.NotEqual; src1 = (Ir.Var "counter");
           src2 = (Ir.Constant 5); dst = (Ir.Var "tmp.3")};
         Ir.JumpIfZero {condition = (Ir.Var "tmp.3"); target = "main.if.en.4"};
         (Ir.Return (Ir.Constant 2)); (Ir.Label "main.if.en.4");
         (Ir.Return (Ir.Constant 0))];
       frame =
       Env.lenv {
         namespace = "main";
         counter = 5;
         offset = -16;
         stack slots = {
           result.1 -> -4,
           tmp.0    -> -8,
           tmp.1    -> -12,
           tmp.3    -> -16,
         }}};
     Ir.StaticVariable {name = "counter"; global = true; init = 0}])
